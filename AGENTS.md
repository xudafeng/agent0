# AGENTS.md

- Use single quotes in JavaScript and TypeScript. Keep JSON valid.
- Use pnpm for package management and scripts.
- Use `.env` for local configuration; keep `.env` ignored and `.env.example` committed.
- Write documentation, comments, and usage instructions in English; keep code and API names in English.
- Keep modules small and explicit; split by responsibility only when the code needs it.
- Build incrementally. Add abstractions only when the current step needs them.
